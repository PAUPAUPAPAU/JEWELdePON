[[5,6,"<title>JEWEL de PON - Prototype Ver.0.9.26</title>\n"],[796,797,"    <div class=\"titleFoot\">Prototype Ver.0.9.26 / ONLINE PvP</div>\n"],[1352,1353,"function practice(){titleScreen.classList.add('hidden');resultScreen.classList.add('hidden');vsActive=false;waitingForStart=false;startCountdownActive=false;grid=emptyGrid();nextId=1;chainSeq=1;resetFlowState();clearingIds=new Set();comboLevel=0;comboExpireAt=0;clearJobs=[];particles=[];flashes=[];stars=[];specialFx=[];conversion=null;score=0;comboState=null;const rows={8:[1,1,2,1,3,4],9:[2,3,4,5,1,2],10:[3,4,5,1,2,3],11:[4,5,1,2,3,4]};Object.entries(rows).forEach(([rr,a])=>a.forEach((v,c)=>{const p=panel(v);p.x=c;p.targetX=c;p.y=+rr;p.targetY=+rr;grid[+rr][c]=p;}));garbage={row:5,y:5,targetY:5,col:0,w:6,h:3,cells:Array(18).fill(0),landT:0}; boardDirty=true;say('0.9.26：2段から接続お邪魔を即一体化・全段変換。');}\n"],[1406,1406,"}\n\nfunction connectedPlayerGarbageGroup(seed){\n  if(!seed)return [];\n  const list=allGarbageObjects();\n  const seen=new Set([seed]),q=[seed],group=[];\n  while(q.length){\n    const a=q.shift();group.push(a);\n    for(const b of list){\n      if(b===a||seen.has(b))continue;\n      if(garbageObjectsTouch(a,b)){seen.add(b);q.push(b);}\n    }\n  }\n  return group;\n}\nfunction mergePlayerGarbageComponentNow(seed){\n  const group=connectedPlayerGarbageGroup(seed);\n  if(group.length<2)return seed;\n  const merged=mergeGarbageGroup(group);\n  const keep=allGarbageObjects().filter(g=>!group.includes(g));\n  garbage=merged;\n  extraGarbages=keep;\n  boardDirty=true;\n  return merged;\n"],[2403,2403,"  // v0.9.26: any touching garbage stack is treated as ONE object from 2 layers onward.\n  // This final merge is done again at conversion start so even two freshly-stacked\n  // 6x1 slabs are guaranteed to convert as one connected block.\n  target=mergePlayerGarbageComponentNow(target);\n"],[2993,2993,"\nfunction connectedCpuGarbageGroup(seed){\n  if(!seed)return [];\n  const list=allCpuGarbageObjects();\n  const seen=new Set([seed]),q=[seed],group=[];\n  while(q.length){\n    const a=q.shift();group.push(a);\n    for(const b of list){\n      if(b===a||seen.has(b))continue;\n      if(cpuGarbageObjectsTouch(a,b)){seen.add(b);q.push(b);}\n    }\n  }\n  return group;\n}\nfunction mergeCpuGarbageComponentNow(seed){\n  const group=connectedCpuGarbageGroup(seed);\n  if(group.length<2)return seed;\n  const merged=mergeCpuGarbageGroup(group);\n  const keep=allCpuGarbageObjects().filter(g=>!group.includes(g));\n  cpuGarbage=merged;\n  cpuExtraGarbages=keep;\n  cpuBoardDirty=true;\n  return merged;\n}\n"],[3302,3302,"  target=mergeCpuGarbageComponentNow(target);\n"]]