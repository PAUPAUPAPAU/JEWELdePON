[[4, 6, "<meta name=\"viewport\" content=\"width=device-width,initial-scale=1,viewport-fit=cover\">\n<title>JEWEL de PON - Prototype Ver.0.9.39</title>\n"], [769, 769, "/* Safe areas and a stable portrait board size for mobile Safari. */\nhtml{-webkit-text-size-adjust:100%;overscroll-behavior:none}\nbody{padding: max(10px,env(safe-area-inset-top)) max(10px,env(safe-area-inset-right)) max(10px,env(safe-area-inset-bottom)) max(10px,env(safe-area-inset-left))}\n.titleScreen,.resultScreen,.subScreen{overflow-y:auto;padding:max(18px,env(safe-area-inset-top)) max(18px,env(safe-area-inset-right)) max(18px,env(safe-area-inset-bottom)) max(18px,env(safe-area-inset-left))}\nbutton{touch-action:manipulation}\n.canvasWrap,.riseWide{ -webkit-touch-callout:none; -webkit-user-select:none;user-select:none }\n@media (pointer:coarse){input{font-size:16px!important}.pauseTopBtn{min-width:44px;width:44px;min-height:44px;height:44px}.opponentTop{height:44px}}\n@supports(height:100svh){@media(pointer:coarse) and (orientation:portrait){.pvpApp{max-width:min(560px,calc((100svh - env(safe-area-inset-top) - env(safe-area-inset-bottom) - 100px)/2 + 100px))}}}\n"], [796, 797, "    <div class=\"titleFoot\">Prototype Ver.0.9.39 / iPhone対応</div>\n"], [1492, 1493, "function practice(){titleScreen.classList.add('hidden');resultScreen.classList.add('hidden');vsActive=false;waitingForStart=false;startCountdownActive=false;grid=emptyGrid();nextId=1;chainSeq=1;resetFlowState();clearingIds=new Set();comboLevel=0;comboExpireAt=0;clearJobs=[];particles=[];flashes=[];stars=[];specialFx=[];conversion=null;score=0;comboState=null;const rows={8:[1,1,2,1,3,4],9:[2,3,4,5,1,2],10:[3,4,5,1,2,3],11:[4,5,1,2,3,4]};Object.entries(rows).forEach(([rr,a])=>a.forEach((v,c)=>{const p=panel(v);p.x=c;p.targetX=c;p.y=+rr;p.targetY=+rr;grid[+rr][c]=p;}));garbage={row:5,y:5,targetY:5,col:0,w:6,h:3,cells:Array(18).fill(0),landT:0}; boardDirty=true;say('0.9.39：コンボ回復を修正・攻撃強化。2分後にピンク、4分後に濃いグレー追加。');}\n"], [2693, 2694, "canvas.addEventListener('pointerdown',e=>{if(drag)return;if(matchFinished||waitingForStart||startCountdownActive||gameOver||playerSpecialPaused()||playerFlipHoldUntil)return;const b=canvas.getBoundingClientRect(),gx=(e.clientX-b.left)/b.width*C,gy=(e.clientY-b.top)/b.height*R,p=findVisualPanel(gx,gy);drag={pointerId:e.pointerId,id:p?.id||null,gx,moved:false,left:b.left,top:b.top,width:b.width,height:b.height};try{canvas.setPointerCapture(e.pointerId)}catch(_){}});\n"], [2709, 2713, "canvas.addEventListener('pointermove',e=>{if(!drag?.id||e.pointerId!==drag.pointerId)return;moveDraggedJewel((e.clientX-drag.left)/drag.width*C);});\ncanvas.addEventListener('pointercancel',e=>{if(!e||e.pointerId===drag?.pointerId)drag=null;});\ncanvas.addEventListener('lostpointercapture',e=>{if(!e||e.pointerId===drag?.pointerId)drag=null;});\ncanvas.addEventListener('pointerup',e=>{if(!drag||e.pointerId!==drag.pointerId)return;const d=drag;drag=null;if(d.moved)return;if(d.id){const f=findLogical(d.id);if(!f)return;if(f.c===C-1){if(!swapAt(d.id,-1))say('左には動かせません。');}else if(!swapAt(d.id,1))say('右には動かせません。');return;}const c=Math.floor((e.clientX-d.left)/d.width*C),r=Math.max(0,Math.min(R-1,Math.floor((e.clientY-d.top)/d.height*R+riseOffset)));if(c<C-1&&!grid[r][c]&&grid[r][c+1])swapAt(grid[r][c+1].id,-1);});\n"], [4355, 4357, "// iOS needs gesture-unlocked Web Audio for effects and dependable gain control.\nfunction createMobileAudioEngine(){\n  const isIOS=/iPad|iPhone|iPod/.test(navigator.userAgent)||(navigator.platform==='MacIntel'&&navigator.maxTouchPoints>1);\n  const Ctx=window.AudioContext||window.webkitAudioContext;\n  if(!isIOS||!Ctx)return null;\n  let context;try{context=new Ctx();}catch(_){return null;}\n  const cache=new Map(),pending=new Map(),queue=[],voices=[];let loading=0,warmed=false;\n  function pump(){\n    while(loading<2&&queue.length){\n      const job=queue.shift();loading++;\n      fetch(job.src).then(r=>{if(!r.ok)throw Error('Audio unavailable');return r.arrayBuffer();})\n        .then(b=>context.decodeAudioData(b)).then(b=>{cache.set(job.src,b);job.resolve(b);},job.reject)\n        .finally(()=>{loading--;pending.delete(job.src);pump();});\n    }\n  }\n  function load(src){\n    if(cache.has(src))return Promise.resolve(cache.get(src));\n    if(pending.has(src))return pending.get(src);\n    const p=new Promise((resolve,reject)=>queue.push({src,resolve,reject}));pending.set(src,p);pump();return p;\n  }\n  function effect(src){\n    const gain=context.createGain();gain.connect(context.destination);\n    let volume=1,position=0,node=null,token=0;\n    const voice={src,preload:'auto',loop:false,paused:true,\n      get volume(){return volume;},set volume(v){volume=v;gain.gain.value=v;},\n      get currentTime(){return position;},set currentTime(v){position=v;},\n      pause(){token++;this.paused=true;if(node){const old=node;node=null;old.onended=null;try{old.stop();}catch(_){}old.disconnect();}},\n      play(){\n        this.pause();this.paused=false;const requested=token;\n        const start=buffer=>{\n          if(requested!==token||this.paused||context.state!=='running')return;\n          const source=context.createBufferSource();source.buffer=buffer;source.loop=this.loop;source.connect(gain);node=source;\n          source.onended=()=>{source.disconnect();if(node===source){node=null;this.paused=true;}};\n          source.start(0,Math.max(0,Math.min(position,buffer.duration)));\n        };\n        if(cache.has(src)){try{start(cache.get(src));return Promise.resolve();}catch(e){return Promise.reject(e);}}\n        return load(src).then(start);\n      }\n    };voices.push(voice);return voice;\n  }\n  function music(src){\n    const media=new Audio(src),gain=context.createGain();gain.gain.value=0;\n    const source=context.createMediaElementSource(media);source.connect(gain);gain.connect(context.destination);\n    let volume=0;\n    // Only volume is virtualized; streaming keeps long BGM out of decoded buffers.\n    Object.defineProperty(media,'volume',{get:()=>volume,set:v=>{volume=v;gain.gain.value=v;}});\n    return media;\n  }\n  return {effect,music,\n    unlock(){try{if(context.state!=='running')context.resume().catch(()=>{});}catch(_){}if(!warmed){warmed=true;for(const v of voices)load(v.src).catch(()=>{warmed=false;});}},\n    suspend(){for(const v of voices)v.pause();try{context.suspend().catch(()=>{});}catch(_){}},\n    context\n  };\n}\n\nconst mobileAudio=createMobileAudioEngine();\nconst bgmNormalAudio=mobileAudio?mobileAudio.music(AUDIO_DATA.bgmNormal):new Audio(AUDIO_DATA.bgmNormal);\nconst bgmDangerAudio=mobileAudio?mobileAudio.music(AUDIO_DATA.bgmDanger):new Audio(AUDIO_DATA.bgmDanger);\n"], [4361, 4362, "function makeSfx(src){const a=mobileAudio?mobileAudio.effect(src):new Audio(src);a.preload='auto';return a;}\n"], [4443, 4444, "  if(mobileAudio)mobileAudio.unlock();\n  if(audioUnlocked){\n    if(mobileAudio)for(const a of [bgmNormalAudio,bgmDangerAudio])if(a.paused){try{a.play().catch(()=>{});}catch(_){}}\n    return;\n  }\n"], [4453, 4453, "document.addEventListener('pointerdown',()=>{if(mobileAudio)unlockAudio();},{capture:true,passive:true});\ndocument.addEventListener('visibilitychange',()=>{if(document.hidden){drag=null;stopRiseHold();if(mobileAudio)mobileAudio.suspend();}});\nwindow.addEventListener('blur',()=>{drag=null;stopRiseHold();});\nwindow.addEventListener('resize',()=>{drag=null;stopRiseHold();});\n"]]
