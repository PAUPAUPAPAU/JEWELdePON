[[5, 6, "<title>JEWEL de PON - Prototype Ver.0.9.34</title>\n"], [796, 797, "    <div class=\"titleFoot\">Prototype Ver.0.9.34 / フリーズ・フリック修正</div>\n"], [1490, 1491, "function practice(){titleScreen.classList.add('hidden');resultScreen.classList.add('hidden');vsActive=false;waitingForStart=false;startCountdownActive=false;grid=emptyGrid();nextId=1;chainSeq=1;resetFlowState();clearingIds=new Set();comboLevel=0;comboExpireAt=0;clearJobs=[];particles=[];flashes=[];stars=[];specialFx=[];conversion=null;score=0;comboState=null;const rows={8:[1,1,2,1,3,4],9:[2,3,4,5,1,2],10:[3,4,5,1,2,3],11:[4,5,1,2,3,4]};Object.entries(rows).forEach(([rr,a])=>a.forEach((v,c)=>{const p=panel(v);p.x=c;p.targetX=c;p.y=+rr;p.targetY=+rr;grid[+rr][c]=p;}));garbage={row:5,y:5,targetY:5,col:0,w:6,h:3,cells:Array(18).fill(0),landT:0}; boardDirty=true;say('0.9.34：コンボ回復を修正・攻撃強化。2分後にピンク、4分後に濃いグレー追加。');}\n"], [1830, 1831, "    const q=Math.max(0,(now-fx.start)/dur);\n"], [2022, 2023, "function drawFx(now){ctx.save();ctx.globalCompositeOperation='lighter';for(let i=flashes.length-1;i>=0;i--){const f=flashes[i],q=Math.max(0,(now-f.t)/f.life);if(q>=1){flashes.splice(i,1);continue;}ctx.globalAlpha=(1-q)*.55;ctx.fillStyle='#fff4a7';ctx.beginPath();ctx.arc(f.x,f.y,(8+25*q)*renderScale,0,Math.PI*2);ctx.fill();ctx.globalAlpha=(1-q)*.22;ctx.fillStyle='#ffb92e';ctx.beginPath();ctx.arc(f.x,f.y,(14+40*q)*renderScale,0,Math.PI*2);ctx.fill();}for(let i=stars.length-1;i>=0;i--){const s=stars[i],q=Math.max(0,(now-s.t)/s.life);if(q>=1){stars.splice(i,1);continue;}ctx.globalAlpha=1-q;ctx.strokeStyle='#fff7bd';ctx.lineWidth=Math.max(1,renderScale);const len=(3+8*q)*renderScale;ctx.beginPath();ctx.moveTo(s.x-len,s.y);ctx.lineTo(s.x+len,s.y);ctx.moveTo(s.x,s.y-len);ctx.lineTo(s.x,s.y+len);ctx.stroke();}ctx.restore();for(let i=particles.length-1;i>=0;i--){const p=particles[i],q=Math.max(0,(now-p.t)/p.life);if(q>=1){particles.splice(i,1);continue;}ctx.globalAlpha=1-q;ctx.fillStyle=p.color;ctx.fillRect(p.x+p.vx*q-2*renderScale,p.y+p.vy*q+18*renderScale*q*q-2*renderScale,4*renderScale,4*renderScale);}ctx.globalAlpha=1;}\n"], [2272, 2273, "    const fx=specialFx[i],dur=Math.max(360,specialPauseMs(fx.type)+180),q=Math.max(0,(now-fx.start)/dur);\n"], [2679, 2680, "function moveDraggedJewel(gx){\n  if(!drag?.id)return;\n  const delta=gx-(drag.lastGX??drag.gx);drag.lastGX=gx;\n  if(Math.abs(gx-drag.gx)>.08)drag.moved=true;\n  if(delta===0)return;\n  const direction=Math.sign(delta);\n  if(drag.direction!==direction){drag.direction=direction;drag.travel=0;drag.step=FLICK;}\n  drag.travel+=Math.abs(delta);\n  // A short flick moves once; continued travel moves one cell per cell width.\n  // Never reverse a swap merely because the one-cell step overshot the finger.\n  for(let n=0;n<C&&drag.travel>=drag.step;n++){\n    if(!swapAt(drag.id,direction)){drag.travel=0;break;}\n    drag.travel-=drag.step;drag.step=1;\n  }\n}\ncanvas.addEventListener('pointermove',e=>{if(!drag?.id)return;moveDraggedJewel((e.clientX-drag.left)/drag.width*C);});\ncanvas.addEventListener('pointercancel',()=>{drag=null;});\ncanvas.addEventListener('lostpointercapture',()=>{drag=null;});\n"], [3435, 3436, "    const fx=cpuSpecialFx[i],dur=Math.max(360,specialPauseMs(fx.type)+180),q=Math.max(0,(now-fx.start)/dur);\n"], [4348, 4348, "const sfxLastPlayed=Object.create(null);\n"], [4350, 4354, "  const a=sfxPool[name];if(!a)return;\n  const now=performance.now();\n  if(now-(sfxLastPlayed[name]??-Infinity)<60)return;\n  sfxLastPlayed[name]=now;\n  // One reusable voice per effect prevents cumulative media allocation and\n  // identical sounds stacking at full volume during a flick or simultaneous clear.\n  try{\n    a.pause();a.currentTime=0;\n    a.volume=Math.max(0,Math.min(1,(SFX_VOL[name]||.5)*scale));\n    const playing=a.play();if(playing)playing.catch(()=>{});\n  }catch(_){}\n"]]
