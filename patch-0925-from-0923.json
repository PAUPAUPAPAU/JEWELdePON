[[5,6,"<title>JEWEL de PON - Prototype Ver.0.9.25</title>\n"],[661,661,"\n/* v0.9.24 dense HTML title: no image asset */\n.htmlLogo{width:min(100%,448px);margin:0 auto 3px;text-align:center}\n.htmlLogoFrame{\n  position:relative;\n  box-sizing:border-box;\n  width:100%;\n  padding:8px 14px 7px;\n  background:#000;\n  border-top:4px solid #238cff;\n  border-bottom:4px solid #21d85a;\n  box-shadow:inset 0 3px 0 #a9e8ff,inset 0 -3px 0 #e4ef45,0 3px 0 #0753a9,0 -3px 0 #08742d;\n}\n.htmlLogoFrame:before,.htmlLogoFrame:after{content:\"\";position:absolute;top:10px;bottom:10px;width:4px;background:#fff;pointer-events:none}\n.htmlLogoFrame:before{left:-1px;box-shadow:3px 0 0 #238cff}\n.htmlLogoFrame:after{right:-1px;box-shadow:-3px 0 0 #21d85a}\n.htmlLogoText{\n  display:flex;\n  align-items:stretch;\n  justify-content:center;\n  gap:0;\n  width:100%;\n  overflow:visible;\n  white-space:nowrap;\n  font-family:Impact,\"Arial Narrow\",\"Arial Black\",\"MS Gothic\",sans-serif;\n  font-size:clamp(39px,10.8vw,62px);\n  font-weight:900;\n  line-height:.82;\n  letter-spacing:-.055em;\n  transform:none;\n}\n.htmlLogoText span{\n  display:inline-block;\n  flex:0 1 auto;\n  color:#ff4a21;\n  background:linear-gradient(#ff9a27 0 24%,#ff4b22 25% 58%,#cf1e18 59% 100%);\n  -webkit-background-clip:text;background-clip:text;-webkit-text-fill-color:transparent;\n  -webkit-text-stroke:1px #5b0d0b;\n  text-shadow:2px 0 0 #5b0d0b,-2px 0 0 #5b0d0b,0 2px 0 #5b0d0b,0 4px 0 #8c100e,3px 4px 0 #130707;\n}\n.htmlLogoSub{margin:17px 0 3px;font-family:\"MS Gothic\",\"Courier New\",monospace;font-size:14px;font-weight:900;letter-spacing:2.4px;color:#fff;text-shadow:2px 2px 0 #27364d}\n\n/* Incoming garbage preview: one line outside the player board */\n.incomingPreview{\n  height:30px;margin:0 0 5px;display:flex;align-items:center;gap:4px;\n  padding:3px 5px;box-sizing:border-box;border:1px solid #80522f;background:#100a07;\n}\n.incomingPreviewLabel{flex:0 0 auto;font:900 7px/1 \"MS Gothic\",monospace;color:#ffe091;letter-spacing:.3px}\n.incomingPreviewCells{display:grid;grid-template-columns:repeat(6,1fr);gap:2px;flex:1;min-width:0;height:16px}\n.incomingPreviewCell{position:relative;border:1px solid #3c2a1b;background:#24170f;box-shadow:inset 0 1px 0 rgba(255,255,255,.08)}\n.incomingPreviewCell.on:after{content:\"\";position:absolute;inset:2px 3px;border-top:2px solid rgba(255,255,255,.45);border-left:1px solid rgba(255,255,255,.18)}\n.incomingPreviewCell.h1{background:#b9914e}.incomingPreviewCell.h2{background:#d5b62d}.incomingPreviewCell.h3{background:#df7224}.incomingPreviewCell.h4{background:#d53c32}\n.incomingPreviewExtra{flex:0 0 18px;text-align:center;font:900 8px/1 \"MS Gothic\",monospace;color:#ff705e}\n\n/* Score / pressure status under opponent mini-board */\n.miniScorePanel,.miniStatusPanel{margin-top:5px;padding:5px 4px;border:1px solid #735039;background:#0d0907;font-family:\"MS Gothic\",\"Courier New\",monospace}\n.miniScoreTitle{font-size:7px;color:#ffe08a;letter-spacing:.5px}.miniScoreValue{margin-top:2px;font-size:12px;line-height:1;color:#fff;font-weight:900;word-break:break-all}\n.miniOpponentScore{margin-top:4px;padding-top:4px;border-top:1px solid #493224;font-size:7px;color:#68dcff;line-height:1.25}\n.miniStatusPanel{font-size:7px;line-height:1.55;color:#ddd}.miniStatusPanel b{color:#ffe08a;font-weight:900}.miniStatusValue{color:#fff}\n\n"],[715,717,"    <div class=\"htmlLogo\" aria-label=\"JEWEL de PON\">\n      <div class=\"htmlLogoFrame\">\n        <div class=\"htmlLogoText\"><span>JEWEL</span><span>de</span><span>PON</span></div>\n      </div>\n      <div class=\"htmlLogoSub\">PUZZLE BATTLE</div>\n    </div>\n"],[732,733,"    <div class=\"titleFoot\">Prototype Ver.0.9.25 / ONLINE PvP</div>\n"],[781,781,"      <div class=\"helpRow\"><b>SCORE</b><span>通常100点／個、特殊効果50点／個。同時消しボーナス＋COMBO倍率</span></div>\n"],[844,844,"      <div id=\"incomingPreview\" class=\"incomingPreview\" aria-label=\"次のお邪魔予告\">\n        <span class=\"incomingPreviewLabel\">NEXT</span>\n        <div class=\"incomingPreviewCells\">\n          <i class=\"incomingPreviewCell\"></i><i class=\"incomingPreviewCell\"></i><i class=\"incomingPreviewCell\"></i>\n          <i class=\"incomingPreviewCell\"></i><i class=\"incomingPreviewCell\"></i><i class=\"incomingPreviewCell\"></i>\n        </div>\n        <span id=\"incomingPreviewExtra\" class=\"incomingPreviewExtra\"></span>\n      </div>\n"],[855,855,"      <div class=\"miniScorePanel\">\n        <div class=\"miniScoreTitle\">SCORE</div><div id=\"playerScoreUi\" class=\"miniScoreValue\">00000000</div>\n        <div class=\"miniOpponentScore\">OPP <span id=\"opponentScoreUi\">00000000</span></div>\n      </div>\n      <div class=\"miniStatusPanel\">\n        <div><b>相殺</b> <span id=\"cancelTotalUi\" class=\"miniStatusValue\">0</span></div>\n        <div><b>次</b> <span id=\"nextGarbageUi\" class=\"miniStatusValue\">なし</span></div>\n        <div><b>予告</b> <span id=\"nextGarbageTimeUi\" class=\"miniStatusValue\">--</span></div>\n        <div><b>待機</b> <span id=\"pendingGarbageUi\" class=\"miniStatusValue\">0</span></div>\n      </div>\n"],[956,956,"const incomingPreview=document.getElementById('incomingPreview'),incomingPreviewCells=[...document.querySelectorAll('.incomingPreviewCell')],incomingPreviewExtra=document.getElementById('incomingPreviewExtra');\nconst playerScoreUi=document.getElementById('playerScoreUi'),opponentScoreUi=document.getElementById('opponentScoreUi'),cancelTotalUi=document.getElementById('cancelTotalUi'),nextGarbageUi=document.getElementById('nextGarbageUi'),nextGarbageTimeUi=document.getElementById('nextGarbageTimeUi'),pendingGarbageUi=document.getElementById('pendingGarbageUi');\n"],[960,961,"const HOLD=320,CLEAR_MS=230,TAP_MS=78,FLICK=.20,CONVERT_GAP=185,FRAME_MS=30,RISE_MS=180,AUTO_STEP_MS=140,COMBO_GRACE=1500,TOP_GRACE=7000,RISE_REPEAT=105,RISE_HOLD=180;\n"],[988,989,"const ATTACK_WARN=2400,ATTACK_MAX_HOLD=8000,ATTACK_DROP_GAP=450;\n"],[990,991,"let playerAttackBank={maxCombo:1,active:false,lastPulse:1},cpuAttackBank={maxCombo:1,active:false,lastPulse:1};\nlet playerCancelledTotal=0,cpuCancelledTotal=0,remoteScore=0,cpuScore=0,vsUiNextAt=0;\n"],[1266,1267,"function practice(){titleScreen.classList.add('hidden');resultScreen.classList.add('hidden');vsActive=false;waitingForStart=false;startCountdownActive=false;grid=emptyGrid();nextId=1;chainSeq=1;resetFlowState();clearingIds=new Set();comboLevel=0;comboExpireAt=0;clearJobs=[];particles=[];flashes=[];stars=[];specialFx=[];conversion=null;score=0;comboState=null;const rows={8:[1,1,2,1,3,4],9:[2,3,4,5,1,2],10:[3,4,5,1,2,3],11:[4,5,1,2,3,4]};Object.entries(rows).forEach(([rr,a])=>a.forEach((v,c)=>{const p=panel(v);p.x=c;p.targetX=c;p.y=+rr;p.targetY=+rr;grid[+rr][c]=p;}));garbage={row:5,y:5,targetY:5,col:0,w:6,h:3,cells:Array(18).fill(0),landT:0}; boardDirty=true;say('0.9.25：接続お邪魔統合・入力負荷軽減・7秒猶予。');}\n"],[1287,1287,"function garbageObjectCells(g){\n  const cells=[];\n  if(!g)return cells;\n  for(let rr=0;rr<g.h;rr++)for(let cc=0;cc<g.w;cc++){\n    const i=rr*g.w+cc;\n    if(!g.cells||g.cells[i]>=0)cells.push([g.row+rr,g.col+cc,g.cells?g.cells[i]:0]);\n  }\n  return cells;\n}\nfunction garbageObjectsTouch(a,b){\n  if(!a||!b)return false;\n  const bs=new Set(garbageObjectCells(b).map(x=>x[0]+','+x[1]));\n  for(const [r,c] of garbageObjectCells(a)){\n    if(bs.has((r+1)+','+c)||bs.has((r-1)+','+c)||bs.has(r+','+(c+1))||bs.has(r+','+(c-1)))return true;\n  }\n  return false;\n}\nfunction mergeGarbageGroup(group){\n  let minR=Infinity,maxR=-Infinity,minC=Infinity,maxC=-Infinity,landT=0;\n  const live=[];\n  for(const g of group){\n    landT=Math.max(landT,g.landT||0);\n    for(const cell of garbageObjectCells(g)){\n      live.push(cell);minR=Math.min(minR,cell[0]);maxR=Math.max(maxR,cell[0]);minC=Math.min(minC,cell[1]);maxC=Math.max(maxC,cell[1]);\n    }\n  }\n  if(!live.length)return group[0];\n  const w=maxC-minC+1,h=maxR-minR+1,cells=Array(w*h).fill(-1);\n  for(const [r,c,stage] of live){\n    const i=(r-minR)*w+(c-minC);\n    cells[i]=Math.max(cells[i],Number.isFinite(stage)?stage:0);\n  }\n  return {row:minR,y:minR,targetY:minR,col:minC,w,h,cells,landT};\n}\nfunction mergeConnectedPlayerGarbage(){\n  if(conversion)return false;\n  const list=allGarbageObjects();\n  if(list.length<2)return false;\n  // Merge only after the individual slabs have visually landed.\n  const settled=list.filter(g=>Math.abs((Number.isFinite(g.y)?g.y:g.row)-g.row)<.003&&Math.abs((Number.isFinite(g.targetY)?g.targetY:g.row)-g.row)<.003);\n  if(settled.length<2)return false;\n  const visited=new Set(),replace=new Map();let changed=false;\n  for(const start of settled){\n    if(visited.has(start))continue;\n    const group=[],q=[start];visited.add(start);\n    while(q.length){\n      const a=q.shift();group.push(a);\n      for(const b of settled){\n        if(visited.has(b)||a===b)continue;\n        if(garbageObjectsTouch(a,b)){visited.add(b);q.push(b);}\n      }\n    }\n    if(group.length>1){\n      const merged=mergeGarbageGroup(group);changed=true;\n      for(const g of group)replace.set(g,merged);\n    }\n  }\n  if(!changed)return false;\n  const rebuilt=[];\n  for(const g of list){\n    const m=replace.get(g)||g;\n    if(!rebuilt.includes(m))rebuilt.push(m);\n  }\n  garbage=rebuilt.shift()||null;extraGarbages=rebuilt;\n  boardDirty=true;\n  return true;\n}\n"],[1702,1707,""],[1827,1827,"  mergeConnectedPlayerGarbage();\n"],[2073,2074,"  const normalPts=baseAttackCount*100;\n  const specialPts=Math.max(0,ids.length-baseAttackCount)*50;\n  const simultaneousBonus=Math.max(0,baseAttackCount-3)*150;\n  const comboMultiplier=Math.min(10,1+(comboLevel-1)*.5);\n  const clearPoints=Math.round((normalPts+specialPts+simultaneousBonus)*comboMultiplier);\n  clearJobs.push({ids,start:now,removed:false,chain:comboLevel,points:clearPoints,scored:false});\n"],[2109,2110,""],[2115,2115,"      if(!job.scored){score+=job.points||0;job.scored=true;markNetSnapshotDirty();}\n"],[2211,2212,"  const hs=22,vs=16;\n"],[2241,2241,"    mergeConnectedPlayerGarbage();\n"],[2339,2340,""],[2341,2342,""],[2344,2347,"canvas.addEventListener('pointerdown',e=>{if(matchFinished||waitingForStart||startCountdownActive||gameOver||playerSpecialPaused()||playerFlipHoldUntil)return;const b=canvas.getBoundingClientRect(),gx=(e.clientX-b.left)/b.width*C,gy=(e.clientY-b.top)/b.height*R,p=findVisualPanel(gx,gy);drag={id:p?.id||null,gx,moved:false,left:b.left,top:b.top,width:b.width,height:b.height};try{canvas.setPointerCapture(e.pointerId)}catch(_){}});\ncanvas.addEventListener('pointermove',e=>{if(!drag?.id)return;const gx=(e.clientX-drag.left)/drag.width*C;let dx=gx-drag.gx;if(Math.abs(dx)>.08)drag.moved=true;let guard=0;while(Math.abs(dx)>=FLICK&&guard++<4){const d=dx>0?1:-1;if(!swapAt(drag.id,d))break;drag.gx+=d;dx=gx-drag.gx;}});\ncanvas.addEventListener('pointerup',e=>{if(!drag)return;const d=drag;drag=null;if(d.moved)return;if(d.id){if(!swapAt(d.id,1))say('右には動かせません。');return;}const c=Math.floor((e.clientX-d.left)/d.width*C),r=Math.max(0,Math.min(R-1,Math.floor((e.clientY-d.top)/d.height*R+riseOffset)));if(c<C-1&&!grid[r][c]&&grid[r][c+1])swapAt(grid[r][c+1].id,-1);});\n"],[2809,2809,"function cpuGarbageObjectCells(g){\n  const cells=[];\n  if(!g)return cells;\n  for(let rr=0;rr<g.h;rr++)for(let cc=0;cc<g.w;cc++){\n    const i=rr*g.w+cc;\n    if(!g.cells||g.cells[i]>=0)cells.push([g.row+rr,g.col+cc,g.cells?g.cells[i]:0]);\n  }\n  return cells;\n}\nfunction cpuGarbageObjectsTouch(a,b){\n  if(!a||!b)return false;\n  const bs=new Set(cpuGarbageObjectCells(b).map(x=>x[0]+','+x[1]));\n  for(const [r,c] of cpuGarbageObjectCells(a)){\n    if(bs.has((r+1)+','+c)||bs.has((r-1)+','+c)||bs.has(r+','+(c+1))||bs.has(r+','+(c-1)))return true;\n  }\n  return false;\n}\nfunction mergeCpuGarbageGroup(group){\n  let minR=Infinity,maxR=-Infinity,minC=Infinity,maxC=-Infinity,landT=0;\n  const live=[];\n  for(const g of group){\n    landT=Math.max(landT,g.landT||0);\n    for(const cell of cpuGarbageObjectCells(g)){\n      live.push(cell);minR=Math.min(minR,cell[0]);maxR=Math.max(maxR,cell[0]);minC=Math.min(minC,cell[1]);maxC=Math.max(maxC,cell[1]);\n    }\n  }\n  if(!live.length)return group[0];\n  const w=maxC-minC+1,h=maxR-minR+1,cells=Array(w*h).fill(-1);\n  for(const [r,c,stage] of live){const i=(r-minR)*w+(c-minC);cells[i]=Math.max(cells[i],Number.isFinite(stage)?stage:0);}\n  return {row:minR,y:minR,targetY:minR,col:minC,w,h,cells,landT};\n}\nfunction mergeConnectedCpuGarbage(){\n  if(cpuConversion)return false;\n  const list=allCpuGarbageObjects();if(list.length<2)return false;\n  const settled=list.filter(g=>Math.abs((Number.isFinite(g.y)?g.y:g.row)-g.row)<.003&&Math.abs((Number.isFinite(g.targetY)?g.targetY:g.row)-g.row)<.003);\n  if(settled.length<2)return false;\n  const visited=new Set(),replace=new Map();let changed=false;\n  for(const start of settled){\n    if(visited.has(start))continue;\n    const group=[],q=[start];visited.add(start);\n    while(q.length){\n      const a=q.shift();group.push(a);\n      for(const b of settled){if(!visited.has(b)&&a!==b&&cpuGarbageObjectsTouch(a,b)){visited.add(b);q.push(b);}}\n    }\n    if(group.length>1){const merged=mergeCpuGarbageGroup(group);changed=true;for(const g of group)replace.set(g,merged);}\n  }\n  if(!changed)return false;\n  const rebuilt=[];for(const g of list){const m=replace.get(g)||g;if(!rebuilt.includes(m))rebuilt.push(m);}\n  cpuGarbage=rebuilt.shift()||null;cpuExtraGarbages=rebuilt;cpuBoardDirty=true;return true;\n}\n"],[3153,3154,"  const cpuNormalPts=baseAttackCount*100;\n  const cpuSpecialPts=Math.max(0,ids.length-baseAttackCount)*50;\n  const cpuSimBonus=Math.max(0,baseAttackCount-3)*150;\n  const cpuMult=Math.min(10,1+(cpuCombo-1)*.5);\n  const cpuPoints=Math.round((cpuNormalPts+cpuSpecialPts+cpuSimBonus)*cpuMult);\n  cpuClearJobs.push({ids,start:now,removed:false,chain:cpuCombo,points:cpuPoints,scored:false});\n"],[3179,3179,"      if(!job.scored){cpuScore+=job.points||0;job.scored=true;}\n"],[3220,3220,"    mergeConnectedCpuGarbage();\n"],[3225,3225,"  mergeConnectedCpuGarbage();\n"],[3278,3279,"  setTimeout(cpuApplyStabilityChecks,TAP_MS+8);\n"],[3533,3534,"    const remainCells=cells-take;\n"],[3536,3537,"    if(remainCells<=0){\n      queue.shift();\n    }else{\n      const repl=packetsFromPower(remainCells).map(np=>({ ...np,createdAt:p.createdAt,readyAt:p.readyAt,forceAt:p.forceAt }));\n      queue.splice(0,1,...repl);\n    }\n"],[3548,3550,"  if(side==='player')playerAttackBank={maxCombo:1,active:false,lastPulse:1};\n  else cpuAttackBank={maxCombo:1,active:false,lastPulse:1};\n}\nfunction pulsePacketForCombo(combo){\n  if(combo>=2&&combo<=5)return {w:6,h:1,cells:6};\n  if(combo>=8&&((combo-8)%3===0))return {w:6,h:1,cells:6};\n  return null;\n}\nfunction commitVsPackets(side,packets,reason=''){\n  if(!packets||!packets.length)return {cancelled:0,remaining:0,total:0};\n  const total=packets.reduce((a,p)=>a+packetCells(p),0);\n  const ownQueue=side==='player'?playerIncoming:cpuIncoming;\n  const res=cancelIncoming(ownQueue,total);\n  const now=performance.now();\n  if(side==='player')playerCancelledTotal+=res.cancelled;else cpuCancelledTotal+=res.cancelled;\n\n  if(isOnlineMatch()&&side==='player'){\n    if(res.remaining>0){\n      const outgoing=(res.cancelled===0)?packets:packetsFromPower(res.remaining);\n      sendNetGameEvent('attack',{packets:outgoing});\n    }\n  }else{\n    const targetQueue=side==='player'?cpuIncoming:playerIncoming;\n    if(res.remaining>0){\n      const outgoing=(res.cancelled===0)?packets:packetsFromPower(res.remaining);\n      enqueuePackets(targetQueue,outgoing,now);\n    }\n  }\n\n  const who=side==='player'?'YOU':'CPU';\n  if(res.cancelled&&res.remaining)cancelInfo.textContent=who+' 相殺 '+res.cancelled+' / 送信 '+res.remaining;\n  else if(res.cancelled)cancelInfo.textContent=who+' 相殺 '+res.cancelled+' / 全相殺';\n  else cancelInfo.textContent=who+' 圧力送信 '+total+' cells'+(reason?' / '+reason:'');\n  return {cancelled:res.cancelled,remaining:res.remaining,total};\n"],[3552,3554,""],[3555,3557,"  bank.active=true;bank.maxCombo=Math.max(bank.maxCombo,combo);\n  const packets=[];\n"],[3558,3559,"  // 4+ simultaneous clears are sent immediately instead of waiting for combo end.\n"],[3561,3562,"    packets.push({w,h:1,cells:w});\n"],[3564,3566,"  // Pressure pulse: preserve the old cumulative 2-5 combo power as four 6-cell rows,\n  // then add one extra row every three combo steps during expert-length chains.\n  const pulse=pulsePacketForCombo(combo);\n  if(pulse&&combo>bank.lastPulse){packets.push(pulse);bank.lastPulse=combo;}\n  if(packets.length)commitVsPackets(side,packets,combo+' COMBO');\n"],[3575,3583,""],[3584,3639,""],[3641,3642,"  return playerSpecialPaused(now)||clearJobs.length>0||conversion||hasPlayerVerticalFall();\n"],[3644,3645,"  return cpuSpecialPaused(now)||cpuClearJobs.length>0||cpuConversion||cpuAnyMotion();\n"],[3703,3703,"function fmtScore(n){return String(Math.max(0,Math.floor(n||0))).padStart(8,'0');}\nfunction updateIncomingPreview(now){\n  const p=playerIncoming[0];\n  incomingPreviewCells.forEach(el=>{el.className='incomingPreviewCell';});\n  incomingPreviewExtra.textContent='';\n  if(!p)return;\n  const s=packetDropShape(p),depth=Math.max(1,Math.min(4,Math.ceil(queuePower(playerIncoming)/6)));\n  for(let i=0;i<Math.min(C,s.w);i++)incomingPreviewCells[i].classList.add('on','h'+depth);\n  if(playerIncoming.length>1)incomingPreviewExtra.textContent='+'+(playerIncoming.length-1);\n}\n"],[3704,3704,"  if(now<vsUiNextAt)return;\n  vsUiNextAt=now+80;\n"],[3716,3720,"  bar(playerIncoming,youWarnBar);bar(cpuIncoming,cpuWarnBar);\n  updateIncomingPreview(now);\n\n  playerScoreUi.textContent=fmtScore(score);\n  opponentScoreUi.textContent=fmtScore(isOnlineMatch()?remoteScore:cpuScore);\n  cancelTotalUi.textContent=String(playerCancelledTotal);\n  pendingGarbageUi.textContent=String(yp)+'マス';\n  if(playerIncoming.length){\n    const p=playerIncoming[0],cells=packetCells(p),s=packetDropShape(p);\n    const rows=Math.max(1,Math.ceil(cells/6));\n    nextGarbageUi.textContent=(rows>1?rows+'段 ':s.w+'列 ')+cells+'マス';\n    const readyAt=Number.isFinite(p.readyAt)?p.readyAt:(p.createdAt+ATTACK_WARN);\n    nextGarbageTimeUi.textContent=Math.max(0,(readyAt-now)/1000).toFixed(1)+'s';\n  }else{\n    nextGarbageUi.textContent='なし';nextGarbageTimeUi.textContent='--';\n  }\n  cpuStateEl.textContent=cpuGameOver?'KO':(isOnlineMatch()?(netRemoteSnapshotAt?(performance.now()-netRemoteSnapshotAt>1800?'LAG':'ONLINE'):'SYNC'):'TEST');\n"],[3726,3727,"  say('TEST：2.4秒後にCPUへ6×1お邪魔が落ちます。');\n"],[3733,3734,"  say('TEST：2.4秒後にYOU側へ6×1お邪魔が落ちます。');\n"],[4159,4159,"    score:score||0,\n"],[4222,4222,"  remoteScore=Number(s.score)||0;\n"],[4749,4749,"  playerCancelledTotal=0;cpuCancelledTotal=0;remoteScore=0;cpuScore=0;vsUiNextAt=0;\n"],[4769,4770,"  cancelInfo.textContent='0.9.24：COMBO中も6マス単位で小刻みに圧力送信 → 2.4秒予告';\n"]]