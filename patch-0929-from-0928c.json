[[5, 6, "<title>JEWEL de PON - Prototype Ver.0.9.29</title>\n"], [796, 797, "    <div class=\"titleFoot\">Prototype Ver.0.9.29 / 分割お邪魔</div>\n"], [1073, 1074, "const ATTACK_WARN=2400;\nconst playerGarbageDelivery={phase:\"ready\"},cpuGarbageDelivery={phase:\"ready\"};\n"], [1339, 1339, "  resetGarbageDelivery(\"player\");\n"], [1352, 1353, "function practice(){titleScreen.classList.add('hidden');resultScreen.classList.add('hidden');vsActive=false;waitingForStart=false;startCountdownActive=false;grid=emptyGrid();nextId=1;chainSeq=1;resetFlowState();clearingIds=new Set();comboLevel=0;comboExpireAt=0;clearJobs=[];particles=[];flashes=[];stars=[];specialFx=[];conversion=null;score=0;comboState=null;const rows={8:[1,1,2,1,3,4],9:[2,3,4,5,1,2],10:[3,4,5,1,2,3],11:[4,5,1,2,3,4]};Object.entries(rows).forEach(([rr,a])=>a.forEach((v,c)=>{const p=panel(v);p.x=c;p.targetX=c;p.y=+rr;p.targetY=+rr;grid[+rr][c]=p;}));garbage={row:5,y:5,targetY:5,col:0,w:6,h:3,cells:Array(18).fill(0),landT:0}; boardDirty=true;say('0.9.29：空き行に合わせてお邪魔を分割。残りは次の消去まで予告で待機。');}\n"], [1942, 1942, "  let occupied=false;\n"], [1945, 1945, "    occupied=true;\n"], [1950, 1951, "  return occupied;\n"], [2219, 2219, "  const incomingClearEligible=!playerBusyForGarbage(now);\n"], [2232, 2232, "  garbageClearResolved(\"player\",incomingClearEligible);\n"], [2451, 2451, "  maintainComboWindow(now);\n"], [2469, 2470, "      // The hidden 13th row uses overflow; no grid access outside 0..R-1.\n      const destination=r===-1?overflow:(r>=0&&r<R?grid[r]:null);\n      if(destination&&c>=0&&c<C&&!destination[c]){\n"], [2474, 2475, "        destination[c]=p;\n"], [2871, 2873, "function playerComboActivity(now=performance.now()){\n  return playerSpecialPaused(now)||!!playerFlipHoldUntil||!!playerFlipPending||\n    clearJobs.length>0||!!conversion||hasPlayerVerticalFall()||\n    allGarbageObjects().some(g=>Math.abs((g.y??g.row)-(g.targetY??g.row))>.003)||\n    hasFloatingPanels()||hasFloatingGarbage()||canOverflowFall()||!!manualRise||!!playerAutoStep;\n"], [2877, 2881, "  // Preserve the existing full grace while board resolution is active.\n  // Ordinary horizontal swaps alone do not extend a combo.\n  const active=playerComboActivity(now);\n"], [3149, 3149, "  let occupied=false;\n"], [3152, 3152, "    occupied=true;\n"], [3157, 3158, "  return occupied;\n"], [3361, 3362, "  for(let r=cpuGarbage.h-1;r>=0;r--)for(let c=0;c<cpuGarbage.w;c++){\n    if(!cpuGarbage.cells||cpuGarbage.cells[r*cpuGarbage.w+c]>=0)cells.push([r,c]);\n  }\n"], [3367, 3367, "  maintainCpuComboWindow(now);\n"], [3371, 3373, "    if(cpuGarbage.cells)cpuGarbage.cells[rr*cpuGarbage.w+cc]=-1;\n    const destination=r===-1?cpuOverflow:(r>=0&&r<R?cpuGrid[r]:null);\n    if(destination&&c>=0&&c<C&&!destination[c]){\n      const p=cpuPanel(rnd());p.x=c;p.targetX=c;p.y=r;p.targetY=r;destination[c]=p;\n"], [3390, 3390, "  const incomingClearEligible=!cpuBusyForGarbage(now);\n"], [3403, 3403, "  garbageClearResolved(\"cpu\",incomingClearEligible);\n"], [3631, 3634, "  const active=cpuSpecialPaused(now)||!!cpuFlipHoldUntil||!!cpuFlipPending||\n    cpuClearJobs.length>0||!!cpuConversion||cpuHasJewelVerticalFall()||\n    allCpuGarbageObjects().some(g=>Math.abs((g.y??g.row)-(g.targetY??g.row))>.003)||\n    cpuHasFloatingPanels()||allCpuGarbageObjects().some(g=>cpuCanGarbageDown(g))||\n    cpuCanOverflowFall()||!!cpuAutoStep;\n"], [3716, 3716, "  resetGarbageDelivery(\"cpu\");\n"], [3787, 3789, "    readyAt:now+ATTACK_WARN\n"], [3803, 3804, "      const repl=packetsFromPower(remainCells).map(np=>({ ...np,createdAt:p.createdAt,readyAt:p.readyAt }));\n"], [3879, 3879, "// One wave uses the fully empty top rows; the rest stays cancellable in the queue.\nfunction garbageDeliveryFor(side){\n  return side==='player'?playerGarbageDelivery:cpuGarbageDelivery;\n}\nfunction resetGarbageDelivery(side){\n  garbageDeliveryFor(side).phase='ready';\n}\nfunction garbageClearResolved(side,eligible){\n  const state=garbageDeliveryFor(side);\n  // A clear that starts after the previous wave settles unlocks one new wave.\n  // Never count a clear that was already running when the wave arrived.\n  if(state.phase==='awaitClear'||(state.phase==='resolving'&&eligible))state.phase='ready';\n}\n"], [3880, 3881, "  return gameOver||playerSpecialPaused(now)||!!playerFlipHoldUntil||!!playerFlipPending||\n    clearJobs.length>0||!!conversion||anyMotion()||hasPlayerVerticalFall()||\n    hasFloatingPanels()||hasFloatingGarbage()||canOverflowFall()||!!manualRise||!!playerAutoStep;\n"], [3883, 3884, "  return cpuGameOver||cpuSpecialPaused(now)||!!cpuFlipHoldUntil||!!cpuFlipPending||\n    cpuClearJobs.length>0||!!cpuConversion||cpuAnyMotion()||cpuHasJewelVerticalFall()||\n    cpuHasFloatingPanels()||allCpuGarbageObjects().some(g=>cpuCanGarbageDown(g))||\n    cpuCanOverflowFall()||!!cpuAutoStep;\n}\nfunction emptyTopRows(side){\n  const board=side==='player'?grid:cpuGrid;\n  const hidden=side==='player'?overflow:cpuOverflow;\n  const blocks=side==='player'?allGarbageObjects():allCpuGarbageObjects();\n  if(hidden.some(Boolean))return 0;\n  let top=R;\n  for(let r=0;r<R;r++)if(board[r].some(Boolean)){top=r;break;}\n  for(const g of blocks){\n    for(let rr=0;rr<g.h;rr++)for(let cc=0;cc<g.w;cc++){\n      if(g.cells&&g.cells[rr*g.w+cc]<0)continue;\n      top=Math.min(top,g.row+rr);\n    }\n  }\n  return Math.max(0,Math.min(R,top));\n}\nfunction readyGarbagePower(queue,now){\n  let power=0;\n  for(const p of queue){\n    const readyAt=Number.isFinite(p.readyAt)?p.readyAt:(p.createdAt??now)+ATTACK_WARN;\n    if(now<readyAt)break;\n    power+=packetCells(p);\n  }\n  return power;\n}\nfunction consumeGarbagePower(queue,power){\n  let left=power;\n  while(left>0&&queue.length){\n    const p=queue[0],count=packetCells(p),take=Math.min(left,count);\n    left-=take;\n    if(take===count)queue.shift();\n    else p.cells=count-take; // Keep the warning age of the undelivered remainder.\n  }\n}\nfunction makeGarbageWave(count){\n  const w=Math.min(C,count),h=Math.ceil(count/C),cells=Array(w*h).fill(-1);\n  let left=count;\n  for(let rr=h-1;rr>=0;rr--){\n    const n=Math.min(w,left),start=Math.floor((w-n)/2);\n    for(let cc=start;cc<start+n;cc++)cells[rr*w+cc]=0;\n    left-=n;\n  }\n  return {row:-h,y:-h,targetY:-h,col:Math.floor((C-w)/2),w,h,cells,landT:0};\n"], [3886, 3889, "  const count=packetCells(packet);\n  return {w:Math.min(C,count),h:Math.ceil(count/C),cells:count};\n"], [3890, 3891, ""], [3892, 3895, "  const count=Math.min(packetCells(packet),emptyTopRows('player')*C);\n  if(count<=0)return false;\n  const g=makeGarbageWave(count);\n"], [3896, 3899, "  settleGarbage();boardDirty=true;\n  say('お邪魔 '+count+'マスが落下。残りは予告で待機します。');\n"], [3901, 3902, ""], [3903, 3906, "  const count=Math.min(packetCells(packet),emptyTopRows('cpu')*C);\n  if(count<=0)return false;\n  const g=makeGarbageWave(count);\n"], [3907, 3909, "  cpuSettleGarbage();cpuBoardDirty=true;\n"], [3910, 3911, ""], [3914, 3915, "  const state=garbageDeliveryFor(receiver);\n  const busy=receiver==='player'?playerBusyForGarbage(now):cpuBusyForGarbage(now);\n  if(state.phase==='resolving'){\n    if(!busy)state.phase='awaitClear';\n    return;\n  }\n  if(!queue.length||state.phase!=='ready'||busy)return;\n  const count=Math.min(readyGarbagePower(queue,now),emptyTopRows(receiver)*C);\n  if(count<=0)return;\n  const spawn=receiver==='player'?spawnPlayerGarbage:spawnCpuGarbage;\n  if(!spawn({cells:count}))return;\n  consumeGarbagePower(queue,count);\n  state.phase='resolving';\n  if(receiver==='player')playerLastGarbageDrop=now;else cpuLastGarbageDrop=now;\n}\n"], [3916, 3941, ""], [3975, 3976, "    const p=playerIncoming[0],cells=Math.min(queuePower(playerIncoming),emptyTopRows('player')*C),s=packetDropShape({cells});\n"], [3977, 3978, "    nextGarbageUi.textContent=cells?((rows>1?rows+'段 ':s.w+'列 ')+cells+'マス'):'空き行待ち';\n"], [3979, 3980, "    const phase=playerGarbageDelivery.phase;\n    nextGarbageTimeUi.textContent=phase==='resolving'?'処理中':phase==='awaitClear'?'次の消去待ち':\n      playerBusyForGarbage(now)?'処理中':cells===0?'空き行待ち':Math.max(0,(readyAt-now)/1000).toFixed(1)+'s';\n"], [4842, 4843, ""], [5014, 5014, "  resetGarbageDelivery(\"player\");resetGarbageDelivery(\"cpu\");\n"], [5036, 5037, "  cancelInfo.textContent='お邪魔は空き行まで落下。残りは次の消去後に投入・相殺可能';\n"], [5183, 5183, "  maintainCpuComboWindow(now);\n"], [5228, 5228, "    // Protect the final busy frame BEFORE its completion flags are cleared.\n    maintainComboWindow(t);\n"]]
